import { siteSettings }      from "./siteSettings";
import { pageMeta }          from "./pageMeta";
import { homepageSections }  from "./homepageSections";

export const schemaTypes = [siteSettings, pageMeta, homepageSections];
