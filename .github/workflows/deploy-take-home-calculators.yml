name: Deploy take-home-calculators to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "take-home-calculators/**"
      - ".github/workflows/deploy-take-home-calculators.yml"
  workflow_dispatch: {}
  repository_dispatch:
    types: [sanity-deploy]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-take-home-calculators"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: take-home-calculators
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: take-home-calculators/package-lock.json

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build static export
        env:
          NODE_ENV: production
          NEXT_PUBLIC_BASE_PATH: ""
          NODE_OPTIONS: "--max-old-space-size=4096"
        run: npm run build

      - name: Write CNAME
        run: echo "salarytools.in" > out/CNAME

      - name: Add .nojekyll
        run: touch out/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ github.workspace }}/take-home-calculators/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
