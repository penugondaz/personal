name: Deploy take-home-calculators to GitHub Pages
on:
  push:
    branches: [main]
    paths:
      - "take-home-calculators/**"
      - ".github/workflows/deploy-take-home-calculators.yml"
  workflow_dispatch:
  repository_dispatch:
    types: [sanity-deploy]
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: "pages-take-home-calculators"
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache Next.js build
        uses: actions/cache@v4
        with:
          path: take-home-calculators/.next/cache
          key: nextjs-${{ hashFiles('take-home-calculators/package-lock.json') }}-${{ hashFiles('take-home-calculators/**/*.ts', 'take-home-calculators/**/*.tsx') }}
          restore-keys: |
            nextjs-${{ hashFiles('take-home-calculators/package-lock.json') }}-

      - name: Install dependencies
        run: cd take-home-calculators && npm install --legacy-peer-deps

      - name: Build static export
        env:
          NODE_ENV: production
          NEXT_PUBLIC_BASE_PATH: ""
          NODE_OPTIONS: "--max-old-space-size=6144"
          NEXT_TELEMETRY_DISABLED: "1"
        run: cd take-home-calculators && npm run build

      - name: Write CNAME and nojekyll
        run: |
          echo "salarytools.in" > take-home-calculators/out/CNAME
          touch take-home-calculators/out/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: take-home-calculators/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
